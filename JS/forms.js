class forms {    form_name = '';    constructor(form) {        this.form_name = form;    }    title = 'Новая форма';    position = undefined;    call_back = function() {    };    box = {};    render() {        let title = this.title;        let call_b = this.call_back;        SENDER('get_form', {name: this.form_name}, function(mess) {            create_window(transform_pos('center'), title, function(form) {                $(form).find('.minimaze-btn').remove();                let inner = $('<div class="inner-content"></div>');                $(inner).html(mess);                $(form).find('.content').append(inner);                setTimeout(call_b(form), 10);            });        });    }}