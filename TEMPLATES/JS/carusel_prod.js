$(document).ready(function() {    let swiper = new Swiper('.swiper', {        direction: 'horizontal',        loop: false,        slidesPerView: 1,        pagination: {            el: '.swiper-pagination',        },        navigation: {            nextEl: '.swiper-button-next',            prevEl: '.swiper-button-prev',        },    });    swiper.on('slideChange', function () {        $('.sel-slide-preview').removeClass('sel-slide-preview');        let currentSlide = swiper.slides[swiper.activeIndex];        // $('.back-shore').attr('style', 'background-image: url("'+$(currentSlide).find('img').attr('src')+'")');        $('#slider-nav img:nth-child('+(swiper.activeIndex+1)+')').addClass('sel-slide-preview');    });    $(document).on('click', '.micro-img', function(e) {        let num = $(this).attr('data-num');        swiper.slideTo(num, 1000, true);    });    $('#slider-nav img:first-child').addClass('sel-slide-preview');});